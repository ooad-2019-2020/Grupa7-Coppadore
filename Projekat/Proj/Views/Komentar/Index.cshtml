@model IEnumerable<Projekat.Models.Komentar>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.komentarTekst)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.datum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.komentarTekst)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KomentarID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KomentarID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KomentarID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
